#include <bits/stdc++.h>
using namespace std;

int main() {
    string amountString = "5000";

    // Convert the string to an integer
    int amount = stoi(amountString);

    cout << "The order amount is: " << amount << endl;
    return 0;
}
